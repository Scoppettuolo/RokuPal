# Prevent PHP execution in uploaded files.
<FilesMatch "\.(?i:php|phtml|phar|php\d)$">
  <IfModule mod_authz_core.c>
    Require all denied
  </IfModule>
  <IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
  </IfModule>
</FilesMatch>
Options -Indexes
